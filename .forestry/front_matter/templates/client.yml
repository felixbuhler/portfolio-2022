---
label: Client
hide_body: true
fields:
- name: client_title
  type: text
  config:
    required: false
  label: Client Title
- name: client_projects
  type: blocks
  label: Client Projects
  template_types:
  - project-per-client
  config:
    min: 
    max: 
